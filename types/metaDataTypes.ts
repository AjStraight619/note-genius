export type ChatMetaData = {
  id: string;
  title: string;
};

export type FolderMetaData = {
  id: string;
  name: string;
};

export type FileMetaData = {
  id: string;
  name: string;
  type: string;
};
